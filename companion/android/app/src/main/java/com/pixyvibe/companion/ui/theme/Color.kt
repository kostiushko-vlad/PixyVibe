package com.pixyvibe.companion.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * PixyVibe design tokens — matches iOS PVi and macOS DesignSystem.
 */
object PVi {
    val Base = Color(0xFF0D0F14)
    val Surface = Color(0xFF161B22)
    val SurfaceHigh = Color(0xFF1C2333)
    val Border = Color(0x0FFFFFFF) // white @ 6%
    val TextPrimary = Color(0xFFE6EDF3)
    val TextSecondary = Color(0xFF8B949E)

    // Accent: Ice → Purple
    val AccentStart = Color(0xFF38BDF8)
    val AccentEnd = Color(0xFF818CF8)
    val AccentSolid = AccentStart

    val Success = Color(0xFF10B981)
    val Orange = Color(0xFFF97316)
}
